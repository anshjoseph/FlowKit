🚀 Quick Start (Just 3 Commands):
# 1. Build
docker build -t python-executor:latest .

# 2. Run
docker run -d \
  --name python-executor \
  -p 8000:8000 \
  python-executor:latest

# 3. Test
curl http://localhost:8000/health
🔒 Secure Production Run:
docker run -d \
  --name python-executor \
  --restart unless-stopped \
  -p 8000:8000 \
  -e POOL_SIZE=8 \
  -e MAX_QUEUE_SIZE=200 \
  -v $(pwd)/logs:/app/logs \
  --security-opt=no-new-privileges:true \
  --cap-drop=ALL \
  --cap-add=NET_BIND_SERVICE \
  --read-only \
  --tmpfs /tmp/executor:size=512M \
  --tmpfs /tmp:size=100M \
  --cpus="2.0" \
  --memory="2g" \
  python-executor:latest
🎯 Key Features:
✅ Two isolated Python environments (app + sandbox)
✅ Base64 code support for large payloads
✅ Resource monitoring (pool/queue utilization)
✅ Security checks for dangerous code patterns
✅ Non-root execution with restricted sandbox user
✅ Automatic temp file cleanup
✅ Health checks and statistics endpoints
All ready to use without docker-compose! 🎉